{
  <% if(componentsDirectory) { %>"directory": "<%=componentsDirectory%>",<% } %>
  "custom": {
    <% if(componentsWebUrl) { %>"webUrl": "<%=componentsWebUrl%>",<% } %>
    <% if(requireConfPaths) { %>"requirejsConfigs": [
      <% _.forEach(requireConfPaths, function(name, index) { %>"<%- name %>"<% if(index + 1 !== requireConfPaths.length) { %>,<% } %><% }); %>
    ]<% }; %>
  }
}